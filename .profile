# Downloads bootstrap.py to the current directory.
# Useful for when you find a buildout without a bootstrap.py
alias bootstrap='wget http://downloads.buildout.org/1/bootstrap.py'

# Show git tree in a gui-like manner.
alias gitlog='git log --graph --decorate --pretty=oneline --abbrev-commit --all'
